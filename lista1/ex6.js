const input = require("readline-sync");
let number = input.question("Digite um numero: ");
for(let i=0; i<number; i--){
if(number[i] >= 1){
    let contagem_regressiva = number[i] - 1
console.log(number[i]);

console.log(contagem_regressiva);
}
break;
}

PERGUNTAR PRO PROFESSOR PQ ELE N TA ESCREVENDO OS OUTROS NUMEROS, CONTINUANDO
