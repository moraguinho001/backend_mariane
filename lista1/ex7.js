const input = require("readline-sync");
let number = input.questionInt("Digite um numero: ");
for(let i=0; i<50; i++){
    if(number[i] >= 1){
    let p =  (number[i]%2 === 0)
    let soma = console.log(p);
    }
}

    ACABAR