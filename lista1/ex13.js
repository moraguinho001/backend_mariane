const input = require("readline-sync");
let inicio = 1;
let fim = 55;
function range(inicio, fim){
    for(let i=inicio; i<=fim; i++){
        let soma = range(inicio, fim);
        i++;
        console.log(soma);
    }
if(inicio = 55){
    inicio = fim;
}
}
